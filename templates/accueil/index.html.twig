{% extends 'base.html.twig' %}

{% block title %}Hello AccueilController!{% endblock %}

{% block body %}


<div class="container mt-5">
    <div class="row">
            <div class="col-md-12">
                <h1 class="text-center">
                    {% for info in param %}
                        {{ info.title }}
                    {% endfor %}
                </h1>
            <div>
                <p class="text-center mt-5">
                    {% for info in param %}
                        {{ info.contentOne|raw }}
                    {% endfor %}
                </p>
     </div>

        <div class="d-flex align-items-center justify-content-center mt-4">
            <img class="logo img-fluid" src="{{ asset('asset/image/imgdoss.png') }}">
        </div>

    </div>

    <div id="carouselExampleFade" class="carousel slide carousel-fade mt-5">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img src={{ asset("asset/image/salle-1.jpeg") }} class="imgSlider d-block w-100" alt="...">
            </div>
            <div class="carousel-item">
                <img src="{{ asset("asset/image/bar-1.jpeg") }}" class="imgSlider d-block w-100" alt="...">
            </div>
            S
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleFade" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleFade" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
        </button>
    </div>
</div>

<div class="container mt-5">
    <div class="d-flex align-items-center justify-content-center">
        <img class="logomenu img-fluid" src="{{ asset('asset/image/imgdoss.png') }}">
        <h2 class="mt-3">Nos plats les plus appréciés</h2>
        <img class="logomenu img-fluid" src="{{ asset('asset/image/imgdoss.png') }}">
    </div>


        <div class="container mt-5 d-flex align-items-center justify-content-center flex-wrap">
            <div class="example">
                <img src="{{ asset("asset/image/pates.webp") }}" height="400" width="600" alt="tree" />
                <div class="content">
                    <div class="text">pâtes</div>
                </div>
            </div>

            <div class="example">
                <img src="{{ asset("asset/image/viande.webp") }}" width="600" height="400" alt="tree" />
                <div class="content">
                    <div class="text">Poulet sur son lit de saison</div>
                </div>
            </div>

            <div class="example">
                <img src="{{ asset("asset/image/boeuf.webp") }}" width="600" height="400" alt="tree" />
                <div class="content">
                    <div class="text">Boeuf savoyard croquant</div>
                </div>
            </div>

            <div class="example">
                <img src="{{ asset("asset/image/pates.webp") }}" width="600" height="400" alt="tree" />
                <div class="content">
                    <div class="text">Arbre</div>
                </div>
            </div>
        </div>

        <div class="d-flex align-items-center justify-content-center mt-4">
            <a href=""><button type="button" class="btn btn-secondary"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Reserver</font></font></button></a>
        </div>


</div>
{% endblock %}
